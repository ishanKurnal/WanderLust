<% if (success && success.length) { %> <!-- Check if there is any success message -->
  <div class="alert alert-success alert-dismissible fade show col-6 offset-3" role="alert">
    <%= success %> <!-- Display the success message -->
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button> <!-- Close button -->
  </div>
<% } %>

<% if (error && error.length) { %> <!-- Check if there is any error message -->
  <div class="alert alert-danger alert-dismissible fade show col-6 offset-3" role="alert">
    <%= error %> <!-- Display the error message -->
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button> <!-- Close button -->
  </div>
<% } %>
